{
  "name": "ccb-admin-server",
  "version": "1.0.0",
  "description": "Local backend for the Christ Church Bluffton admin panel — handles privileged Supabase operations (team account creation/removal/password resets) that require the secret key, which must never touch browser code.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}

# RETIRED 2026-08-05 — the admin panel is no longer its own separate Netlify site.
# It's part of this same christ-church-bluffton deploy now: pages live in src/admin/
# and are published alongside the public site, reachable at christchurchbluffton.org/admin.
# The real config is the repo-root netlify.toml (clean URLs come from that file's
# pretty_urls setting, CSP there is extended to allow Supabase + jsdelivr, and it has
# a dedicated /admin/* noindex header). This file is kept only so Netlify doesn't error
# if anything still points at this path — it is not read by the actual build.
